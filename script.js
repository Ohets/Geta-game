let reactionTimer,reactReady=false,reactStart;
function reaction(){const box=document.getElementById("reactionResult");box.textContent="Warte auf GO…";reactReady=false;clearTimeout(reactionTimer);reactionTimer=setTimeout(()=>{reactReady=true;reactStart=performance.now();box.textContent="🟢 JETZT!";},1000+Math.random()*3000)}
document.addEventListener("click",e=>{if(reactReady&&e.target.closest("button")){const ms=Math.round(performance.now()-reactStart);document.getElementById("reactionResult").textContent="⚡ "+ms+" ms";reactReady=false}});
let spaceRun;
function space(){const b=document.getElementById("spaceBox");b.innerHTML='<div id="ship">🚀</div><div class="score" id="spaceScore">Score: 0</div>';let ship=b.querySelector("#ship"),score=0, x=50;function move(e){if(e.key==="ArrowLeft")x=Math.max(8,x-8);if(e.key==="ArrowRight")x=Math.min(92,x+8);ship.style.left=x+"%"}document.onkeydown=move;clearInterval(spaceRun);spaceRun=setInterval(()=>{score++;document.getElementById("spaceScore").textContent="Score: "+score;if(score>=30){clearInterval(spaceRun);b.innerHTML='<strong>🏆 Überlebt! Score '+score+'</strong><br><button onclick="space()">Nochmal</button>'}},1000)}
function memory(){const r=document.getElementById("memoryResult");const a=["🍎","🚀","⭐","🐱","🍎","🚀","⭐","🐱"].sort(()=>Math.random()-.5);let open=[],done=0;r.innerHTML="";a.forEach(v=>{const b=document.createElement("button");b.textContent="❓";b.style.margin="4px";b.onclick=()=>{if(open.length>=2||b.dataset.done)return;b.textContent=v;open.push([b,v]);if(open.length===2){if(open[0][1]===open[1][1]){open.forEach(x=>x[0].dataset.done=1);done+=2;open=[];if(done===8)r.insertAdjacentHTML("beforeend","<p>🏆 Geschafft!</p>")}else setTimeout(()=>{open.forEach(x=>x[0].textContent="❓");open=[]},600)}};r.appendChild(b)})}
function aim(){const b=document.getElementById("aimBox");let n=0,start=performance.now();b.innerHTML='<div id="target"></div><div class="score" id="aimScore">0 / 10</div>';function next(){const t=document.getElementById("target");if(!t)return;t.style.left=Math.random()*80+"%";t.style.top=Math.random()*65+"%";t.onclick=()=>{n++;document.getElementById("aimScore").textContent=n+" / 10";if(n>=10){b.innerHTML="<strong>🎯 "+Math.round(performance.now()-start)/1000+" s</strong><br><button onclick=\"aim()\">Nochmal</button>"}else next()}}next()}